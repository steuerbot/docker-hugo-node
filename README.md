# Docker Hugo Node
This is a Dockerfile for an hugo-dockerimage with nodejs support. It's based on ubuntu:bionic - so beware of the size 😉.

To build just pull the repo and use `docker build ...` to build the image.
